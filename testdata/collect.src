a = 101;
b = 102;
c = "103";
d = [1, 2, a, b, c+b, "abc"];

add = x -> {
	while (x > 0) {
		x = x - 1;
		if (x == 5) {
			continue;
		} else if (x == 3) {
			return len(d) ** 2;
			break;
		}
	}
	return x;
};

export(add(a));
